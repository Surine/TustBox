<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="@dimen/block_tool_height"
    android:id="@+id/box_background"
    android:layout_margin="1dp"
    android:background="?android:attr/selectableItemBackgroundBorderless"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView
        android:id="@+id/box_image"
        android:layout_width="@dimen/block_tool_icon_height"
        android:layout_height="@dimen/block_tool_icon_height"
        android:src="@mipmap/ic_launcher"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"/>

    <TextView
        android:id="@+id/box_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/box_image"
        android:layout_marginBottom="18dp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:text="TUST"
        android:textColor="@color/common_google_signin_btn_text_dark_focused"
        />


</RelativeLayout>
